version: '3.8'

services:
  e2e-test:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      # Mount the project directory so changes are reflected without rebuild
      - .:/workspace
    # Keep container running for interactive debugging if tests fail
    stdin_open: true
    tty: true
